implement word rules in get_word();
modify shlvl at program start
if string is empty do nothing
ignore empty strings

	BUILTSINS
cd with only a relative or absolute path
export with no options
unset with no options
env with no options or arguments
exit with no options

update old pwd with ft_cd
ft_env dont print empty envs;

handle single quotes in expansion
pwd builtin shouldnt rely on env

env_init
make shlvl do +1
make empty oldpwd
ask about _ in env
